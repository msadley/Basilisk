{
  "name": "@basilisk/test",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "npx tsc -b",
    "dev": "nodemon --watch 'src/**/*.ts' --watch '../../packages/**/*.ts' --exec 'TS_NODE_PROJECT=tsconfig.dev.json node -r ts-node/register -r tsconfig-paths/register src/index.ts'",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@abraham/reflection": "^0.13.0",
    "@basilisk/core": "*",
    "@libsql/client": "^0.17.4"
  }
}
